<!--分享提示浮层-->
<template>
  <div class="dvd-service-com-web-share-guide" :style="styleObject" @click="close" @touchmove="touchmove">
    <img class="arrow" src="http://pic.davdian.com/free/pointer.png" v-if="showArrow">
    <div class="tip" @click.prevent.stop>
      <template v-if="isWeixin">
        <p>请点击右上角“分享”按钮,</p>
        <p>然后可以选择“发送给朋友”、“分享到朋友圈”或者“复制链接”后发送链接给朋友。</p>
      </template>
      <template v-if="!isWeixin">
        <p v-html="browserTip || '请复制地址栏链接，将链接发送给朋友'"></p>
      </template>
    </div>
  </div>
</template>

<script>
  import ua from 'dvd-base-js-ua';

  export default {
    data: function () {
      return {
        styleObject: {
          display: 'block'
        },
        isWeixin: ua.isWeiXin(),
      }
    },
    props: {
      classObject: {},
      type: String,  // 取值weixin
      browserTip: {
        type: String,
        default: null
      },
      // 是否显示箭头
      showArrow: {
        type: Boolean,
        default: true,
      },
    },
    components: {},
    methods: {
      show(){
//        this.styleObject.display = 'block';
      },
      close(){
//        this.styleObject.display = 'none';
        this.$destroy();
        this.$el.parentNode.removeChild(this.$el);
      },
      touchmove(event){
        event.preventDefault();
      },
    },
    filters: {},
    computed: {},
    watch: {},
    created: function () {
    },
    mounted: function () {
    }
  }
</script>

<style lang="sass" lang="scss" rel="stylesheet/scss">
  @import "../dvd-base-scss-util/dvd-base-scss-util";

  // 分享提示浮层
  .dvd-service-com-web-share-guide {
    position: fixed;
    bottom: 0;
    width: 100%;
    max-width: 640px;
    height: 100%;
    background-color: #000;
    opacity: 0.8;
    z-index: 99;
    display: none;
    .arrow {
      position: absolute;
      top: 0;
      right: r(30);
    }
    .tip {
      margin-top: r(100);
      color: #ffffff;
      font-size: r(15);
      padding: 0 r(30);
      text-align: center;
      line-height: 1.5;
      user-select: text;
    }
  }

</style>
